import 'package:flutter/foundation.dart';

import 'tile_stack_decorator.dart';

class RemoveFromStackDecorator extends TileStackDecorator {
  RemoveFromStackDecorator(
    super.tileStack,
  );

  void removeByKey(Key key) {
    super.tileStack;
  }
}
